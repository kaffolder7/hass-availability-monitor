## 
# Valid Configuration
# 
# Confirms the script behaves as expected with valid configuration.
# 
# Mock Endpoint:
# You can use a simple mock API server like `httpbin.org` or run a local server to simulate a successful API response:
# `python3 -m http.server 8000`
# 
# Expected Behavior:
#   - The script logs success on the first attempt.
#   - No “down” or “recovery” notifications are sent.
# 
# Test command:
# `../check_endpoint.sh > valid_configuration.log 2>&1`
# 
# Verify:
#   - Check the logs for a success message:
#     `grep "API request succeeded" valid_configuration.log`
# 
HASS_API_URL="http://localhost:8000"  # Simulate a working local server
HASS_AUTH_TOKEN="valid_token"
SMS_TO="+1234567890"
SMS_FROM="sender@example.com"
ENDPOINT_DOWN_MESSAGE="API is down."
ENDPOINT_UP_MESSAGE="API is back online."