## 
# Invalid SMS Configuration
# 
# This test ensures the script handles invalid SMS settings gracefully.
# 
# Expected Behavior:
#   - The script logs errors when trying to send SMS due to missing SMS_TO or invalid Twilio credentials.
#   - Continues monitoring the API endpoint despite the notification failure.
# 
# Test command:
# `../check_endpoint.sh > invalid_sms.log 2>&1`
# 
# Verify:
#   ```
#   grep "Error: SMS_TO is missing" invalid_sms.log
#   grep "Failed to send SMS" invalid_sms.log
#   ```
# 
HASS_API_URL="http://localhost:8000/valid"
HASS_AUTH_TOKEN="dummy_token"
SMS_TO=""
SMS_FROM="sender@example.com"
ENDPOINT_DOWN_MESSAGE="API is down."
ENDPOINT_UP_MESSAGE="API is back online."
NOTIFICATION_PROVIDER="twilio"
TWILIO_ACCOUNT_SID="invalid_sid"
TWILIO_AUTH_TOKEN="invalid_token"